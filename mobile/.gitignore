# Rust build artifacts
speedrun-ffi/target/

# SwiftPM / Xcode build artifacts (incl. the generated xcframework binary)
ios/.build/
ios/build/
ios/.swiftpm/
*.xcframework
DerivedData/

# Xcode per-user state (the project itself is generated from project.yml and IS
# committed, but user-specific state must not be)
xcuserdata/
*.xcuserstate
